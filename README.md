To test this for yourself, navigate to the project directory in the command line,
and enter 'make interpreter' to compile using the makefile script.

To run simply enter './interpreter' into the command line and it will spit out whatever data was passed into main().

To remove the compiled file use 'make clean'.